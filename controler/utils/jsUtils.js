/*public function leave(ws, clientid, partyid){
    if (!ws instanceof WebSocket){
        throw new Error("called leave with " + ws.className + " element !");
    }

    data = {
        cid: clientid,
        pid: partyid,
        action: "LEAVE",
    }

    ws.send(JSON.stringify(data));*/
}